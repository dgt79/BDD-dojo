package bdd.dojo.simple;

/**
 * This class is where we delegate all of the DSLs/APIs/Utilities/etc that allow us to
 * test the application.  This class is not connected to the test framework in anyway
 * so if we change the framework we only need to change the <small> fixture class and
 * this class will contain the core of the tests.
 */
public class SimpleDriver {
}
